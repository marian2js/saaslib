{
  "name": "saaslib",
  "author": "marian2js",
  "license": "MIT",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --parallel",
    "lint": "npm run lint --workspaces --parallel"
  }
}
