```js
// @ts-ignore
const { androidId } = Expo.Constants;
```
```js
// @ts-ignore
const { androidId } = Expo.Constants;
```
// @expo-cli/configure: Dropped support for Android SDK 21 and 22. ([#24201](https://github.com/expo/expo/pull/24201) by [@behenate](https://github.com/behenate))
```
```
// @expo-cli/configure: Dropped support for Android SDK 21 and 22. ([#24201](https://github.com/expo/expo/pull/24201) by [@behenate](https://github.com/behenate))
```
import { hashAssetFiles } from '@expo/webpack-config';

hashAssetFiles.httpServerLocation = hashAssetFiles.httpServerLocation.replace(
  /^\.\.\//,
  '_'
);
```
```js
import { hashAssetFiles } from '@expo/webpack-config';

hashAssetFiles.httpServerLocation = hashAssetFiles.httpServerLocation.replace(
  /^\.\.\//,
  '_'
);
```
